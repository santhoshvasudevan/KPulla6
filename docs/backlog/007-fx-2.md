# 007 — FX-2: Cash FX warnings and gap hardening

**ID:** FX-2  
**Branch:** `agent/007-fx-2`  
**Depends on:** 006

## Goal

Harden **FX gap handling** on cash and overview surfaces: consistent warnings when display-currency conversion is partial, aligned with summary/performance FX-fill semantics.

## Scope

- Standardize warning payload shape on cash overview/balances (e.g. `fx_warnings[]` with `currency_pair`, `date`, `message`)
- Cash page + overview API: show banner when totals are partial
- Align with `fx/lookup.convert_amount_with_fill` and inverse-pair loading (see FD-ACC-10A-FX-TERMINAL-FIX notes)
- Regression tests: inverse-only FX rows, missing rate dates, multi-currency all-scope
- Update `docs/current-state.md` accepted limitations if same-portfolio FX limitation is resolved
- `docs/changelog.md`

## Do not implement

- New FX sync providers or live read-path fetch
- Investment valuation changes outside cash display
- Transfer fees

## Safety requirements

- Read-path and display logic — backup only if migrations required
- Never run destructive DB commands

## Expected files / areas

| Area | Files |
|------|--------|
| FX lookup | `backend/fx/lookup.py` |
| Cash services | `backend/cash/` |
| Tests | `backend/tests/test_cash_overview_api.py`, `backend/tests/test_all_scope_fx_terminal_alignment.py` (extend if relevant) |
| Frontend | `frontend/src/pages/Cash.jsx` |
| Docs | `docs/cash-ledger.md`, `docs/changelog.md` |

## Tests / commands

```bash
cd backend && DJANGO_TEST_USE_SQLITE=1 .venv/bin/python -m pytest tests/test_cash_overview_api.py tests/test_fx_sync.py -q
make test-backend
make test-critical
git diff --stat
```

## Final response format

1. Task ID: `007 — FX-2`
2. Files changed
3. Tests run (commands + pass/fail)
4. `git diff --stat`
5. Commit hash
6. Deferred items
7. Safety notes
